///The tolower90 function returns the lowercase equivalent of ch if ch is a letter; otherwise ch is returned unchanged
#include<ctype.h>
#include<stdio.h>
int main(void){
    putchar(tolower('Q'));
    return 0;
}
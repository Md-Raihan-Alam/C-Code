/* The ceil() function returns the smallest integer (represented as a double) that is not less than num, For example,
given 1.02, ceil() would return 2.0, while9-1.02 ceil() would return -1*/
#include<stdio.h>
#include<math.h>
int main(void)
{
    printf("%f",ceil(9.1));
    return 0;
}